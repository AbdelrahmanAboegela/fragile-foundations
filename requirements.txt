# Exact versions used to produce the results and figures in this repository.
#
# torch/torchvision are installed separately via the CUDA index, not from
# this file:
#   pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 --index-url https://download.pytorch.org/whl/cu121
# Installing them from a plain `pip install -r requirements.txt` risks
# pip's resolver silently substituting a non-CUDA build on a later pass.

numpy==2.2.6
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.7.2

# Data I/O for HEST-Bench (.h5 patches, .h5ad expression)
h5py==3.16.0
anndata==0.11.4
scanpy==1.11.5  # required by HEST's st_dataset.py loader (imports scanpy at module level)
openslide-python==1.4.6
openslide-bin==4.0.1.2
Pillow==12.3.0
opencv-python-headless==5.0.0.93

# Foundation model / HF access
huggingface_hub==1.31.0
transformers==5.17.0
timm==1.0.29

# Metrics
lpips==0.1.4
pytorch-msssim==1.0.0
scikit-image==0.25.2

# Gene sets / pathway scoring
gseapy==1.3.1

# Utils
pyyaml==6.0.3
tqdm==4.70.1
matplotlib==3.10.9
seaborn==0.13.2

# HEST-Bench itself is a separate dependency, installed via:
#   git clone https://github.com/mahmoodlab/HEST external/HEST
#   pip install -e "external/HEST[benchmark]"
